{
  "name": "mindful-app",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:frontend": "pnpm --filter frontend dev",
    "dev:backend": "pnpm --filter backend dev",
    "dev": "pnpm --filter shared build && pnpm --parallel --stream --filter frontend --filter backend run dev",
    "build": "pnpm recursive run build",
    "clean": "pnpm -r exec rimraf node_modules dist && rimraf node_modules && pnpm store prune"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rimraf": "^6.1.0"
  },
  "packageManager": "pnpm@10.23.0"
}